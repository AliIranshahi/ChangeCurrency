export interface fetchURL {
  url: string;
}

export type typeData = {
  Currency: string;
  Price: string;
  Changes: string;
  Ok: boolean;
  Source: string;
};

export interface Children {
  children: React.ReactNode;
}
